{% block stylesheets %}
    <style>
        table, caption, th, td {
            border: 2px solid skyblue;
            border-collapse: collapse;
        }
        caption, th, td {
            padding: 8px;
        }
        caption {
            background-color: lightgrey;
        }
        th {
            background-color: blue;
        }
        td {
            background-color: lightblue;
        }
    </style>

{% endblock %}
<!DOCTYPE html>


<html>

<head>
    <meta charset="UTF-8">
    <title>Liste des Contrats</title>


</head>

<body>
<img src="C:/Users/wissa/OneDrive/Desktop/download .jfif" alt="My Image">
<h1>Liste des Contrats</h1>
<table>
    <thead>
    <tr>

        <th>ID</th>
        <th>Dates ignature</th>
        <th>Date expiration</th>
        <th>Montant</th>

    </tr>
    </thead>
    <tbody>
    {% for p in contrat %}
        <tr>
            <td>{{ p.id }}</td>
            <td>{{ p.datesignature|date("d/m/Y") }}</td>
            <td>{{ p.dateexpiration|date("d/m/Y") }} Kg</td>
            <td>{{ p.montant }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

</body>
</html>